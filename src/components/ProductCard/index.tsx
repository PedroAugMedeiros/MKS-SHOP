import React from 'react'
import { API } from '../../services/api';
import { useApi } from '../../hooks/useApi';


interface PokeCardProps {
  product: {
    url: string;
    id: string;
    name: string;
  }
}
export default function ProductCard(props: PokeCardProps) {
      
  return (
    <div>
      aawdadwad
    </div>
  )
}
